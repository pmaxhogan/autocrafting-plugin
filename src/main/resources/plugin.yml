name: ${name}
version: ${version}
description: ${description}
author: ${author}
website: ${url}
api-version: 1.17


main: ${mainClass}

commands:
    example:
        description: An example command
        usage: /<command>

permissions: {}