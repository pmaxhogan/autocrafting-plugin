name: ${name}
version: ${version}
description: ${description}
author: ${author}
website: ${url}
api-version: 1.17


main: ${mainClass}

commands:
    sidebar:
        description: Sets which field displays on your sidebar
        usage: /<command> {clear|display} <Name of board to display>

permissions: {}