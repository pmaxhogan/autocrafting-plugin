name: ${name}
version: ${version}
description: ${description}
author: ${author}
website: ${url}
api-version: 1.17


main: ${mainClass}

commands:
  sidebar:
    description: Sets which field displays on your sidebar
    usage: /<command> {clear|display|shffle} <Name of board to display>
  sbar-reload:
    description: Reloads the config
    permission: sidebar.reload
    usage: /<command> {clear|display|shffle} <Name of board to display>

permissions:
  sidebar.reload:
    permission:
      default: op